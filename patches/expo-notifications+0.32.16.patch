diff --git a/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/notifications/model/NotificationContent.java b/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/notifications/model/NotificationContent.java
index 191b64e..dae15e9 100644
--- a/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/notifications/model/NotificationContent.java
+++ b/node_modules/expo-notifications/android/src/main/java/expo/modules/notifications/notifications/model/NotificationContent.java
@@ -103,6 +103,21 @@ public class NotificationContent implements Parcelable, Serializable, INotificat
   @Nullable
   @Override
   public Object getImage(@NonNull Context context, @NonNull Continuation<? super Bitmap> $completion) {
+    // Per-notification icon: read drawable name from data.notifIcon
+    if (mBody != null) {
+      try {
+        String iconName = mBody.optString("notifIcon", "");
+        if (!iconName.isEmpty()) {
+          int resId = context.getResources().getIdentifier(iconName, "drawable", context.getPackageName());
+          if (resId != 0) {
+            return BitmapFactory.decodeResource(context.getResources(), resId);
+          }
+        }
+      } catch (Exception e) {
+        Log.e("expo-notifications", "Could not load per-notification icon.", e);
+      }
+    }
+    // Fallback: manifest meta-data
     try {
       ApplicationInfo ai = context.getPackageManager().getApplicationInfo(context.getPackageName(), PackageManager.GET_META_DATA);
       if (ai.metaData.containsKey(META_DATA_LARGE_ICON_KEY)) {
